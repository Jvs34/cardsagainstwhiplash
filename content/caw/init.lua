--content shit, haven't decided yet
--get local path from {...} ?



local caw = {
	DATABASEPATH = "database/database.json"
	QUESTIONPICKS = 1, --only allow "pick" 1
}


function caw:LoadDatabase()
	--load the json path
end

return {
	
}